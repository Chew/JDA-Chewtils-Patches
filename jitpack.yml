before_install:
   - git config --global user.email "no-reply@gitpack.io"
   - git config --global user.name "JitCI"

install:
   - ./chewtil patch
   - cd JDA-Chewtils
   - chmod u+x ./gradlew
   - ./gradlew clean install
